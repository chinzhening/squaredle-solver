[project]
name = "squaredle-automation"
version = "0.1.0"
description = "Python automation for solving squaredle"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "selenium>=4.46.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.21",
]